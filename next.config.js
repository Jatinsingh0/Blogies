/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.pexels.com",
        "educationinprogress.eu",
        "inspirationfeed.com",
        "ik.imagekit.io"

      ],
      },
}

module.exports = nextConfig
