/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.pexels.com",
        "educationinprogress.eu",
        "inspirationfeed.com",

      ],
      },
}

module.exports = nextConfig
